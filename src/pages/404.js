import * as React from 'react';
import Layout from '../components/Layout';

const NotFoundPage = () => {
  return (
    <Layout>
      <p>Khang</p>
    </Layout>
  );
};

export default NotFoundPage;
